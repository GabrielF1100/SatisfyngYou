/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';

import Navegation from './src/pages/Navegation';

AppRegistry.registerComponent(appName, () => Navegation);
